CREATE TABLE categorias(id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,nome VARCHAR(100) NOT NULL,descricao VARCHAR(500) NOT NULL,usuario_id BIGINT NOT NULL CHECK(usuario_id>0),data_cadastro DATE NOT NULL);
